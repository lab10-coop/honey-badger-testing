


/*

import { LogFileManager } from './logFileManager';

console.log(`${typeof process.env.NODE_ENV} - ${process.env.NODE_ENV}`);
console.log(LogFileManager.getOutputPathCSV());
*/



var myArgs = process.argv.slice(2);

console.log('args: ', myArgs);
